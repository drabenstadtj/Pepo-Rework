extends layout

block content
  .page
    h1.title Leaderboard
    div(style='overflow-x:auto;')
      table.leaderboard-table
        thead
          tr
            th.table-header Rank
            th.table-header Name
            th.table-header Title
            th.table-header Liquid Assets
            th.table-header Invested Assets
            th.table-header Net Worth
        tbody#leaderboard-body
        // The table body will be populated dynamically
    script.
      const backendPort = "#{BACKEND_PORT}";
    script(src="/javascripts/leaderboard.js")
